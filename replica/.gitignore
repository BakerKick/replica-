# Generated by build-standalone.py — ~3.7 MB build artifacts, not source.
# The glob also covers the one-off variants built to compare a change side
# by side (…-DAYBREAK.html and friends), which are the same class of thing.
Shiraume-*.html
